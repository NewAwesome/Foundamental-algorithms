"use strict";
var dp = function (row, col) {
    // define: dp[][]
    var dp = new Array();
    // initialize: dp[][]
    dp.map(function (m) { return m.map(function (sm) {
        sm = 1;
    }); });
    console.log(dp);
    return 123;
};
dp()